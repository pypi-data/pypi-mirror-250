[build-system]
requires = [ "setuptools" ]
build-backend = "setuptools.build_meta"

[project]
name = "vfs_build"
readme = "README.md"
version = "2024.01.10.0"
authors = [ { name="Hugo Leclerc", email="hugo.leclerc@universite-paris-saclay.fr" } ]
description = "C++ builder for VFS"

[project.scripts]
vfs_build = "vfs_build:cli_main"

