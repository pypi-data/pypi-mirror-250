[tool.poetry]
name = "sorting_algorithm"
version = "0.2.0"
description = ""
authors = ["jay.sathiya <jay.sathiya@crestdatasys.com>"]
readme = "README.md"
packages = [{include="sorting_algorithm"}]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
