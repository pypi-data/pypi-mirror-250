[tool.poetry]
name = "test-repo-v2"
version = "v0.0.12"
description = ""
authors = []
packages = [
    { include = "src" },
]
readme="README.md"

[tool.poetry.dependencies]
python = ">=3.8"
pydantic = "^1"

[tool.poetry.group.release.dependencies]
GitPython = "^3.1"
requests = "^2.31"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry_bumpversion.file."./src/hello.py"]