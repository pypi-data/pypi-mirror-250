[tool.poetry]
name = "fauna-auth"
version = "0.0.9"
description = "Helper library"
authors = ["Edward FitzGerald <edward.fitzgerald@fetch.ai>"]
readme = "README.md"
packages = [{include = "fauna_auth"}]

[tool.poetry.dependencies]
python = "^3.9"
fastapi = "^0.97.0"
jwcrypto = "^1.5.0"

[tool.poetry.group.dev.dependencies]
black = "^23.3.0"
pytest = "^7.4.0"
coverage = "^7.3.0"

[tool.poetry.group.test.dependencies]
uvicorn = "^0.23.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
