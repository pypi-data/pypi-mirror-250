[tool.poetry]
name = "cria-core"
version = "0.1.0"
description = ""
authors = ["Andrei Fajardo <andrei@nerdai.io>"]
readme = "README.md"
packages = [{include = "cria_index/core"}]

[tool.poetry.dependencies]
python = "^3.10"
pytest = "^7.4.4"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
