[build-system]
requires = ["setuptools", "wheel"]

[project]
name = "antimatter-engine"
version = "0.0.1"
description = "A package for working with Antimatter containers"
readme = "README.md"
authors = [{ name = "Michael Andersen", email = "michael@antimatter.io" }]

