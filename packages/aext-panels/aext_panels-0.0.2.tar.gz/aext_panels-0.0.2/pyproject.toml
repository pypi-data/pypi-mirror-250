[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aext-panels"
dynamic = ["version"]
description = 'The aext-panels component of anaconda-toolbox'
readme = "README.md"
license = "BSD-3-Clause"

[tool.hatch.version]
path = "src/aext_panels/__init__.py"
