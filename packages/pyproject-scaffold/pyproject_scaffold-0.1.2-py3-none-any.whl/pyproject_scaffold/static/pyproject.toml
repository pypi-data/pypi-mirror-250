[build-system]
requires = ["setuptools", "wheel"]

[project]
name = "example-name"
version = "0.1.0"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest",
    #  "pytest-mock",
    #  "pyfakefs",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
namespaces = false
