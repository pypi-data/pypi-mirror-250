from setuptools import setup, find_packages

setup(
    name='guiauthlibrary',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'Django',
    ],
    author='Shiwani Kasar',
   
)
