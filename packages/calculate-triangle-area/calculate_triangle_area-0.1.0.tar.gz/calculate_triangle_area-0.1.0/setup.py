from setuptools import setup

setup(
    name='calculate_triangle_area',
    version='0.1.0',
    py_modules=['triangle_area'],
    install_requires=[
        # List any dependencies here if needed
    ],
)