[tool.poetry]
name = "nh5"
version = "0.1.0"
description = "Utilities to support the nh5 file format"
authors = ["Jeremy Magland"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
h5py = "^3.10.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
