[tool.poetry]
name = "pyke_hooks"
version = "0.1.0"
description = ""
authors = ["Andrey Castro <andreyyec@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.9"
PyYAML = "^6.0.1"
deepmerge = "^1.1.1"

[tool.poetry.scripts]
atlantis-yaml-generate = "pyke_hooks.atlantis_yaml_generate:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
