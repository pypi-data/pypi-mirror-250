from setuptools import setup, find_packages

setup(
    name='bug_tracker',
    version='0.1.5',
    packages=find_packages(),
    entry_points={
        'console_scripts': [
            # Add any command-line scripts here
        ],
    },
)
