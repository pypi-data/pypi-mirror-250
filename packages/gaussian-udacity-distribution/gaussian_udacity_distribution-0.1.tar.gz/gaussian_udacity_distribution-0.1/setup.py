from setuptools import setup

setup(name='gaussian_udacity_distribution',
      version='0.1',
      description='Gaussian distributions',
      packages=['gaussian_udacity_distribution'],
      zip_safe=False)
