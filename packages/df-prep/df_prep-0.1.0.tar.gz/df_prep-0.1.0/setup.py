from setuptools import setup, find_packages

setup(
    name='df_prep',  # Replace with your library name
    version='0.1.0',
    author='DF',
    author_email='your_email@example.com',
    packages=find_packages(),
    install_requires=[],  # List any dependencies here
)